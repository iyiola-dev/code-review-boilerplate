[project]
name = "code-review-crew"
version = "0.1.0"
description = "A multi-agent code reviewer built with Google ADK."
requires-python = ">=3.10"
dependencies = [
    "google-adk>=1.0.0",
]

[tool.uv]
package = false
